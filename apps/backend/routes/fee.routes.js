import express from "express";import auth from "../middleware/auth.js";import authorize from "../middleware/authorize.js";import {createFee,deleteFee,listFees,listMyFees,updateFee} from "../controllers/fee.controller.js";const r=express.Router();
r.get("/me", auth, authorize("player"), listMyFees);r.get("/",auth,authorize("admin","coach"),listFees);r.post("/",auth,authorize("admin","coach"),createFee);r.put("/:id",auth,authorize("admin","coach"),updateFee);r.delete("/:id",auth,authorize("admin"),deleteFee);export default r;
