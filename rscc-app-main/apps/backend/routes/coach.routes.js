import express from "express";import auth from "../middleware/auth.js";import authorize from "../middleware/authorize.js";import {createCoach,deleteCoach,listCoaches,updateCoach} from "../controllers/coach.controller.js";const r=express.Router();r.get("/",auth,listCoaches);r.post("/",auth,authorize("admin"),createCoach);r.put("/:id",auth,authorize("admin"),updateCoach);r.delete("/:id",auth,authorize("admin"),deleteCoach);export default r;
